import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  mainInput: {
    backgroundColor: '#FFF',
    height: 60,
    paddingHorizontal: 20,
    borderRadius: 10,
    fontSize: 18,
    fontWeight: '600',
    marginBottom: 20,
  },
});
